apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: ecommerce

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: ecommerce-storage

  resources:
    requests:
      storage: 2Gi